FROM maven:3.8.5-jdk-11-slim

WORKDIR notification-sender

COPY . .

ENTRYPOINT mvn -s maven-settings.xml -U install DPOSTGRES_USERNAME=${POSTGRES_USERNAME} DPOSTGRES_PASSWORD=${POSTGRES_PASSWORD} DPOSTGRES_URL=${POSTGRES_URL}
