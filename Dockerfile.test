FROM maven:3.8.5-jdk-11-slim

WORKDIR notification-sender

COPY . .

ENTRYPOINT mvn -s maven-settings.xml -U install POSTGRES_USERNAME=${POSTGRES_USERNAME};POSTGRES_PASSWORD=${POSTGRES_PASSWORD};POSTGRES_URL=${POSTGRES_URL}
